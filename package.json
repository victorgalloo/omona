{
  "name": "omona",
  "private": true,
  "workspaces": [
    "apps/server",
    "apps/dashboard",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "dev:server": "npm run dev --workspace=apps/server",
    "dev:dashboard": "npm run dev --workspace=apps/dashboard",
    "build:server": "npm run build --workspace=apps/server",
    "build:dashboard": "npm run build --workspace=apps/dashboard",
    "start": "cd apps/server && npx tsx src/index.ts",
    "video:studio": "npm --prefix apps/video run studio",
    "video:render": "npm --prefix apps/video run render",
    "video:install": "npm --prefix apps/video install"
  },
  "devDependencies": {
    "turbo": "^2",
    "typescript": "^5.7"
  },
  "packageManager": "npm@10.9.4",
  "engines": {
    "node": ">=20"
  }
}
